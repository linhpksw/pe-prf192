#include <math.h>
#include <stdio.h>

int main() {
    int i, n, arr[50];

    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

        // printf("OUTPUT:\n%d", sum);

    return 0;
}
