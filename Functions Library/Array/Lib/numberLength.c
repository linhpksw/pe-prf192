#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_ELEMENT 20

int numberLength(int arr[]);

int main() {
    // int arr[MAX_ELEMENT] = {-1, 5, 6, 8, 3, 8, 6, 5, 1, -7};
    int arr[MAX_ELEMENT];

    int size = sizeof(arr) / sizeof(int);

    // printf("%d\n", sizeof(arr));
    // printf("%d", size);
    // int size = numberLength(arr);

    // printf("%d\n", size);

    return 0;
}

int numberLength(int arr[]) {
    int i = 0, size = 0;

    return size;
}
