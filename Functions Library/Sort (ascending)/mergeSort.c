#include <stdio.h>
#include <stdlib.h>

void mergeSort(float a, int start, int end)
{
    if (start < end)
    {
        int mid = start + (end - start) / 2;
    }
}

int main()
{

    system("pause");
    return 0;
}
